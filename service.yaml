apiVersion: v1
kind: Service
metadata:
  name: bank-service 
  labels:
    name: bank-service   
spec:
  ports:
    - name: http
    - port: 8458
  selector:
    app: bank-service
  type: NodePort
  
